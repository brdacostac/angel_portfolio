---
const galleryItems = [
  { src: "/images/work_photo3.png", alt: "The Foreigner - Act 2: Storytime", caption: "The Foreigner - Act 2: Storytime", objectPosition: "left center" },
  { src: "/images/work_photo1.png", alt: "The Moors – Act 1: Emile's Song", caption: "The Moors – Act 1: Emile's Song", objectPosition: "75% center" },
  { src: "/images/work_photo2.png", alt: "The Moors – Act 2: Exchange", caption: "The Moors – Act 2: Exchange" },
];
---

<section class="gallery section" id="gallery">
  <div class="container">
    <div class="gallery-header reveal">
      <span class="section-label">Gallery</span>
      <h2>My work in <em>pictures</em></h2>
    </div>

    <div class="gallery-grid">
      {galleryItems.map((item, i) => (
        <button
          class={`gallery-item reveal reveal-delay-${i + 1}`}
          data-src={item.src}
          data-alt={item.alt}
          type="button"
        >
          <img
            src={item.src}
            alt={item.alt}
            width="600"
            height="450"
            loading="lazy"
            style={item.objectPosition ? `object-position: ${item.objectPosition}` : undefined}
          />
          <div class="gallery-overlay">
            <span class="gallery-caption">{item.caption}</span>
          </div>
        </button>
      ))}
    </div>
  </div>

  <!-- Lightbox -->
  <div class="lightbox" id="lightbox" role="dialog" aria-label="Image viewer" aria-hidden="true">
    <button class="lightbox-close" aria-label="Close image viewer">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" aria-hidden="true">
        <path d="M18 6L6 18M6 6l12 12" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
      </svg>
    </button>
    <img class="lightbox-img" src="" alt="" />
  </div>

  <div class="section-line"></div>
</section>

<style>
  :global(#gallery) {
    background: var(--bg-alt);
  }

  .gallery-header {
    margin-bottom: var(--space-lg);
  }

  .gallery-header h2 {
    font-size: clamp(2.5rem, 5vw, 4rem);
    line-height: 1.05;
  }

  .gallery-header h2 em {
    color: var(--accent);
    font-style: italic;
  }

  .gallery-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-xs);
  }

  .gallery-item {
    position: relative;
    overflow: hidden;
    cursor: pointer;
    aspect-ratio: 4 / 5;
    border: 1px solid var(--border);
    padding: 0;
    background: var(--plum-mid);
  }

  .gallery-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.8s var(--ease-out);
  }

  .gallery-item:hover img {
    transform: scale(1.06);
  }

  /* Overlay sempre visível na base — o usuário vê o título sem precisar hover */
  .gallery-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to top,
      rgba(15, 6, 8, 0.72) 0%,
      rgba(15, 6, 8, 0.1) 35%,
      transparent 65%
    );
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    justify-content: flex-end;
    padding: var(--space-sm) var(--space-md);
    transition: background 0.5s var(--ease);
  }

  .gallery-item:hover .gallery-overlay {
    background: linear-gradient(
      to top,
      rgba(15, 6, 8, 0.9) 0%,
      rgba(30, 12, 22, 0.25) 45%,
      transparent 100%
    );
  }

  .gallery-caption {
    color: rgba(255, 255, 255, 0.9);
    font-size: 0.78rem;
    font-weight: 400;
    letter-spacing: 0.04em;
    /* sem uppercase — os títulos têm travessões e dois-pontos */
    line-height: 1.4;
  }

  /* Lightbox */
  .lightbox {
    position: fixed;
    inset: 0;
    z-index: 200;
    background: rgba(15, 6, 8, 0.96);
    backdrop-filter: blur(24px);
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.4s var(--ease);
  }

  .lightbox.active {
    opacity: 1;
    pointer-events: auto;
  }

  .lightbox-close {
    position: absolute;
    top: var(--space-md);
    right: var(--space-md);
    background: none;
    border: 1px solid rgba(255,255,255,0.25);
    color: white;
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all var(--duration) var(--ease);
  }

  .lightbox-close:hover {
    border-color: white;
    color: white;
    background: rgba(255,255,255,0.1);
  }

  .lightbox-img {
    max-width: 85vw;
    max-height: 85vh;
    object-fit: contain;
  }

  .section-line {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--border), transparent);
  }

  @media (max-width: 768px) {
    .gallery-grid {
      grid-template-columns: 1fr 1fr;
    }

    .gallery-item:first-child {
      grid-column: 1 / -1;
      aspect-ratio: 16 / 10;
    }
  }

  @media (max-width: 480px) {
    .gallery-grid {
      grid-template-columns: 1fr;
    }

    .gallery-item:first-child {
      grid-column: auto;
      aspect-ratio: 4 / 5;
    }
  }
</style>

<script>
  const lightbox = document.getElementById('lightbox');
  const lightboxImg = lightbox?.querySelector('.lightbox-img') as HTMLImageElement;
  const lightboxClose = lightbox?.querySelector('.lightbox-close');

  document.querySelectorAll('.gallery-item').forEach((item) => {
    item.addEventListener('click', () => {
      const src = item.getAttribute('data-src');
      const alt = item.getAttribute('data-alt');
      if (lightboxImg && src) {
        lightboxImg.src = src;
        lightboxImg.alt = alt || '';
        lightbox?.classList.add('active');
        lightbox?.setAttribute('aria-hidden', 'false');
      }
    });
  });

  function closeLightbox() {
    lightbox?.classList.remove('active');
    lightbox?.setAttribute('aria-hidden', 'true');
  }

  lightboxClose?.addEventListener('click', closeLightbox);
  lightbox?.addEventListener('click', (e) => {
    if (e.target === lightbox) closeLightbox();
  });

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') closeLightbox();
  });
</script>
